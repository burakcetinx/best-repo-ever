<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent_Account__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <externalId>false</externalId>
    <inlineHelpText>This field is used to map the Parent Account from a Lead as it converts to an Account such that the hierarchy can be linked once it is an Account. It is only visible to AMs, Team Leads, and Systems Administrators.</inlineHelpText>
    <label>Parent Account Mapping</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Accounts (Parent Account)</relationshipLabel>
    <relationshipName>Accounts1</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
